{% extends "CdsBandBundle::layout.html.twig" %}
{% block body %}
<h1>Band list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Nationality</th>
            <th>Image</th>
            <th>Presentation</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>   
        <tr>
            <td><a href="{{ path('band_show', { 'id': band.id }) }}">{{ band.id }}</a></td>
            <td>{{ band.name }}</td>
            <td>{{ band.nationality }}</td>
            <td><img src="{{ asset(band.webPath) }}" /></td>
            <td>{{ band.presentation }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('band_show', { 'id': band.id }) }}">show</a>
                    </li>
                    <li>
                        
                    </li>
                </ul>
            </td>
        </tr>
    </tbody>
</table>

<ul>
    <li>
        <span  id="new-band">
            Create a new entry
        </span>
    </li>
</ul>
{% endblock %}